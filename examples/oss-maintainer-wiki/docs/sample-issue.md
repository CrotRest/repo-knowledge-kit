# rkk audit does not mention my release checklist

The CLI output seems incomplete when I run `rkk audit`.

It should include release docs, but I only see README details.

Actual output is missing `docs/release.md`.
